import time
import webbrowser
count_breaks = 0
while ( count_breaks < 2 ) :
    time.sleep(2)
    webbrowser.open("https://translate.google.ps/?hl=en&tab=wT#en/ar/Snoring");
    count_breaks = count_breaks + 1
    
